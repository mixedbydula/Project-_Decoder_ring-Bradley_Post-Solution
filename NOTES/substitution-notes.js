/* Notes 3

Declare Alphabet variable

Randomize alphabet and include new characters, numbers

Maintain spaces

The alphabet parameter must be a string of exactly 26 characters, 
which could include special characters such as #, $, *, etc. 
Otherwise, it should return false.

All the characters in the alphabet parameter must be unique. 
Otherwise, it should return false.
*/

/*
substitution("thinkful", "xoyqmcgrukswaflnthdjpzibev"); //> 'jrufscpw'

Word input & randomized alphabet

abcdefghijklmnopqrstuvwxyz
xoyqmcgrukswaflnthdjpzibev

Iterate over letters of word
For each char, 
match the normal alphabet index with the randomized alpha index

Encode:
Take Thinkful & Return jrufscpw

Iterate over letters of word

For each char, 
match the normal alphabet index with the randomized alpha index

Return Randomized Output

Decode:
Take jrufscpw & Return Thinkful
Iterate over letters of word
For each char, 
match the normal alphabet index with the randomized alpha index

Return Normal Output

*/